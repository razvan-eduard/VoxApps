<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the Android-required app label lives here — all other UI text lives in
         assets/translations/*.json, loaded via LanguageManager (see utils/Strings.kt). -->
    <string name="app_name">Vox Calendar</string>
    <!-- Widget-picker label — read by Android's "add widget" UI before the app's own
         LanguageManager-driven UI is ever shown, so this stays a plain Android string resource. -->
    <string name="calendar_widget_label">Vox Calendar</string>
</resources>
