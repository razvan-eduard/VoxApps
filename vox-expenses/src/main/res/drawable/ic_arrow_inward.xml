<?xml version="1.0" encoding="utf-8"?>
<!-- Arrow pointing down-left into a corner (money arriving) — used for TransactionDirection.INCOMING
     in both the home-screen widget (Glance/RemoteViews can't render Compose Material icons directly)
     and mirrors the in-app Icons.AutoMirrored.Filled.CallReceived path exactly, so both surfaces show
     the same glyph. Color is baked in (not tinted at runtime) since direction color is fixed. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FF4CAF50"
        android:pathData="M20,5.41 L18.59,4 L7,15.59 V9 H5 V19 H15 V17 H8.41 Z" />
</vector>
