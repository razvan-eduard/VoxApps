package com.voxapps.expenses.domain.localization

/**
 * Languages Vox Expenses ships a translation file for (see `assets/translations/`) and offers in the
 * language picker — kept in sync with vox-commander's/vox-notes' own supported set.
 */
object SupportedLanguages {
    val ALL = listOf("en", "ro", "de", "fr")
}
