package com.voxapps.expenses.utils

/**
 * Technical ID constants for the localization system (mirrors vox-notes' Strings.kt). All
 * user-facing text lives in the assets/translations JSON files, never here.
 */
object Strings {
    object Translations {
        const val DIR = "translations/"
        const val DIR_LIST = "translations"
        const val JSON_EXTENSION = ".json"
    }

    object Languages {
        const val DEFAULT = "en"
    }
}
